# # # NOT WORKING

class kokot:
    def access(self):
        print(type(super(picus)))
#        print(super(picus).member)
#         print(super().method())

class picus:
    member = "kundafix"
    fpici = kokot()

    def method(self):
        return "picojebat"



n = picus()
n.fpici.access()

# super()
# Return a proxy object that delegates method calls to a parent or sibling class of type.
#
# This means, super() is only usable with class inheritance, not property-object relations.
